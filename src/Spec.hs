module Spec where
import PdePreludat
import Library
import Test.Hspec

{-
correrTests :: IO ()
correrTests = hspec $ do
  describe "Test de Kata 0" $ do
    it "Test de f para numeros grandes" $ do
      f 6 `shouldBe` 6
    it "Test de f para numeros intermedios - valor limite superior" $ do
      f 5 `shouldBe` 4
    it "Test de f para numeros intermedios - valor limite inferior" $ do
      f 0 `shouldBe` (-1)
    it "Test de f para numeros negativos" $ do
      f (-1) `shouldBe` 1

-}